{
  "name": "@tile/guessing-game",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm test",
    "test": "vitest",
    "tsc": "tsc -p tsconfig.lib.json",
    "build": "vite build && pnpm tsc"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "typescript": "^5.5.3",
    "vite": "^5.4.1",
    "vitest": "^2.0.5"
  }
}
